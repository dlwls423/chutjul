export type ParsedRow=Record<string,string>;

export function cleanExtractedText(text:string){const labels=/^(신청번호|신청경로|진행상황|신청일시|민원제목|<첨부파일>|민원종류|처리완료예정일|설정된 처리기간|민원요지|민원공개여부|열람범위|관리유형|처리결과|처리부서|관리부서|전화번호|관련법령|처리자|처리결과 통보일|민원인 답변 확인일|민원만족도)/;const lines=text.split(/\r?\n/).map(line=>line.replace(/[ \t]+/g,' ').trim()).filter(line=>line&&!/^\d+\.$/.test(line)&&!/^\d{1,2}\.\s*\d{1,2}\.\s*\d{1,2}\.\s*(오전|오후).*gov\.epeople/i.test(line)&&!/^\[?https?:\/\/gov\.epeople/i.test(line)&&!/^\d+\/\d+$/.test(line));const out:string[]=[];for(const line of lines){const previous=out.at(-1);const join=previous&&!labels.test(line)&&!/^[-•가-힣]\.|^\d+\./.test(line)&&!/[.?!:>]$/.test(previous)&&/[가-힣]$/.test(previous)&&/^[가-힣]/.test(line);if(join)out[out.length-1]=`${previous}${line}`;else out.push(line);}return out.join('\n').replace(/\n{3,}/g,'\n\n').trim();}
function valueAfter(text:string,label:string,pattern='[^\\n]+'){const match=text.match(new RegExp(`${label}\\s*(${pattern})`));return match?.[1]?.trim()||'';}
function section(text:string,start:string,ends:string[]){const index=text.search(new RegExp(start));if(index<0)return'';const after=text.slice(index).replace(new RegExp(`^[\\s\\S]*?${start}\\s*`),'');let end=after.length;for(const marker of ends){const found=after.search(new RegExp(marker));if(found>=0&&found<end)end=found;}return cleanExtractedText(after.slice(0,end));}
function maskKoreanName(name:string){const clean=name.replace(/\s/g,'');if(clean.length<2)return clean;if(clean.length===2)return`${clean[0]}O`;return`${clean[0]}${'O'.repeat(clean.length-2)}${clean.at(-1)}`;}
export function parseEpeopleComplaint(raw:string,fileName:string):ParsedRow|null{if(!raw.includes('민원 상세')&&!/신청번호\s*1AA-/.test(raw))return null;const text=cleanExtractedText(raw);const question=section(text,'<첨부파일>[\\s\\S]*?안녕하세요\\.',['민원종류\\s*법령질의','민원처리기간','처리완료예정일']);const answerStart=text.search(/처리결과\s*1\.\s*안녕하세요/);const answer=answerStart>=0?section(text.slice(answerStart),'처리결과',['처리결과 첨부파일','민원만족도','만족도 조사']):'';const attachmentSection=section(text,'<첨부파일>',['안녕하세요\\.']);const attachments=attachmentSection.match(/[^\n]+?\.(?:pdf|hwp|hwpx|xlsx?|docx?)/gi)?.map(x=>x.replace(/\s+/g,' ').trim())||[];const handler=valueAfter(text,'담당자','[가-힣]{2,4}');const processor=valueAfter(text,'처리자','[가-힣]{2,4}');const relatedLaw=valueAfter(text,'관련법령','[^\\n]+');return{title:valueAfter(text,'민원제목')||fileName.replace(/\.pdf$/i,''),question:`안녕하세요.\n${question}`.trim(),answer,content:'',linked_pdf_name:fileName,application_number:valueAfter(text,'신청번호','[A-Z0-9-]+'),receipt_number:valueAfter(text,'접수번호','[A-Z0-9-]+'),application_at:valueAfter(text,'신청일시','\\d{4}-\\d{2}-\\d{2}\\s+\\d{2}:\\d{2}(?::\\d{2})?'),received_at:valueAfter(text,'접수일시','\\d{4}-\\d{2}-\\d{2}\\s+\\d{2}:\\d{2}(?::\\d{2})?'),expected_completion_at:valueAfter(text,'처리완료예정일:','\\d{4}-\\s*\\d{2}-\\d{2}\\s+\\d{2}:\\d{2}:\\d{2}').replace(/-\s+/g,'-'),summary:valueAfter(text,'민원요지'),attachments:JSON.stringify(attachments),handler_masked:maskKoreanName(handler),processor_masked:maskKoreanName(processor),department:valueAfter(text,'처리부서','[^\\n]+?추진\\s*단|[^\\n]+'),notification_at:valueAfter(text,'처리결과 통보일','\\d{4}-\\d{2}-\\d{2}\\s+\\d{2}:\\d{2}'),answer_confirmed_at:valueAfter(text,'민원인 답변 확인일','\\d{4}-\\d{2}-\\d{2}\\s+\\d{2}:\\d{2}'),related_laws:JSON.stringify(relatedLaw?[relatedLaw]:[]),application_channel:valueAfter(text,'신청경로','[^\\n]+'),complaint_kind:valueAfter(text,'민원종류','[^\\n]+'),processing_result:valueAfter(text,'처리구분','[^\\n]+'),public_status:valueAfter(text,'민원공개여부','[^\\n]+'),processing_period_days:valueAfter(text,'설정된 처리기간:','\\d+'),notification_method:valueAfter(text,'민원답변통지방식','[^\\n]+')};}
